<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}AdrarClassroom{% endblock %}</title>
    {% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('styles/stylesheets.css') }}">
    {% endblock %}
</head>

<body>
    <header>
        <nav class="navbar">
            <div class="navbar-container">
                <a class="navbar-brand">AdrarClassroom</a>
                <ul class="navbar-links">
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('user_dashboard') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('chapters_list') }}">Chapitres</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('courses_list') }}">Cours</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link logout" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
    </header>

    <main>
        {% block body %}
        {% endblock %}
    </main>
</body>
</html>
